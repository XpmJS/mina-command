//  自动生成 %%datetime%%
//  import World from '../common/world.js';  // 引用

let web = getWeb();
Page({
	data:{},
	
	/**
	 * 页面加载完毕后执行
	 * @param  {[type]} get [description]
	 * @return {[type]}     [description]
	 */
	onReady: function( get ) {

		// **** 页面的脚本请写在这里  **** 
		console.log("page ready", get );
	},

	hello: function ( event ) {
	},

	world: null
})